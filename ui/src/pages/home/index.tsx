import { FC } from 'react';
import { Main } from '@layouts';
import { Box } from '@mui/material';

const Home: FC = () => {
	return (
		<Main>
			<Box>
				<h1>Home</h1>
			</Box>
		</Main>
	);
};

export default Home;
